/**
 * 此文件已被废弃
 * 
 * 请从 './ant' 目录导入所有UI组件:
 * import { Button, Input, Table } from '@/app/components/ui/ant';
 * 
 * @deprecated 请直接从 './ant' 导入组件
 */

throw new Error(`
[组件导入错误] 从 '@/app/components/ui' 导入已被废弃。
请直接从 '@/app/components/ui/ant' 导入UI组件，例如:
import { Button, Input, Table } from '@/app/components/ui/ant';
`); 