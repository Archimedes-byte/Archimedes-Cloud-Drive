/**
 * 数据库模块 (Database Module)
 * 
 * 此模块负责管理数据库连接和提供数据访问接口。
 * 主要功能：
 * - 提供Prisma ORM客户端实例
 * - 确保在不同环境中正确处理数据库连接
 * - 提供数据库操作的统一入口
 */

// 导出Prisma客户端
export { prisma } from './prisma';

// 导出常用数据库操作工具
export { 
  // 在这里可以添加其他数据库相关的导出
  // 例如：查询构建器、事务助手等
}; 