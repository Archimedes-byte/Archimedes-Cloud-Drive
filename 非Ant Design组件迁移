import { Download, Folder } from 'lucide-react';
import { DownloadOutlined, FolderOutlined } from '@ant-design/icons';
import { Button } from '@ant-design/icons';
import { DeleteOutlined } from '@ant-design/icons';

// 迁移前：使用 Lucide React 图标
<Download size={16} />

// 迁移后：使用 Ant Design 图标
<DownloadOutlined />

// 迁移前：使用原生 button
<button className={buttonStyles.topButton} onClick={onDelete}>
  <Trash2 className="w-4 h-4" />
  删除
</button>

// 迁移后：使用 Ant Design Button
<Button 
  className={buttonStyles.topButton} 
  onClick={onDelete}
  icon={<DeleteOutlined />}
  type="text"
>
  删除
</Button> 